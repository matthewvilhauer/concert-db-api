"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Band');
const Fixture = require('../../fixtures/BandFixture');

describe('models:Band', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
