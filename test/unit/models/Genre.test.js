"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Genre');
const Fixture = require('../../fixtures/GenreFixture');

describe('models:Genre', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
