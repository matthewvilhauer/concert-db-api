"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Concert');
const Fixture = require('../../fixtures/ConcertFixture');

describe('models:Concert', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
