"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ConcertController');

describe('controllers:ConcertController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
