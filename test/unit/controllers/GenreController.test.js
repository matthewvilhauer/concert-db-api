"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/GenreController');

describe('controllers:GenreController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
