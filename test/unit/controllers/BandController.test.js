"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/BandController');

describe('controllers:BandController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
